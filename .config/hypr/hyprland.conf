# monitor=XWAYLAND0,1920x1080@60,0x0,1
exec-once=dunst
exec-once=dropbox
exec-once=waybar
exec-once=swww init
exec-once=set-theme $(get-theme --theme) $(get-theme --mode)
# exec-once=/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
# exec-once=systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

input {
    repeat_rate=25

    touchpad {
        disable_while_typing=true
        natural_scroll=false
        clickfinger_behavior=true
        middle_button_emulation=false
        tap-to-click=true
    }
}

gestures { 
    workspace_swipe=true 
    workspace_swipe_min_speed_to_force=5
    workspace_swipe_invert=false
}

general {
    layout=master
    sensitivity=1.0 # for mouse cursor
    main_mod=SUPER

    gaps_in=5
    gaps_out=20
    border_size=2
    col.active_border=0xff5e81ac
    col.inactive_border=0x66333333

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

    #R:wmage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=10
    active_opacity=.9
    inactive_opacity=.9
    blur=true
    blur_size=8 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    blur_new_optimizations = true   
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}

animations {
    enabled=1
    animation=windows,1,6,default
    animation=workspaces,1,6,default
    animation=border,1,6,default
    animation=fade,1,6,default
}

master {
    new_is_master=false
    new_on_top=false
    no_gaps_when_only=false
}

misc {
    disable_hyprland_logo=true
    disable_splash_rendering=true
    mouse_move_enables_dpms=true
}


# Float
windowrule=float,Rofi
windowrule=float,thunar
windowrule=size 860 580,thunar
windowrule=center,thunar
windowrule=float,pavucontrol
windowrule=size 860 580,pavucontrol
windowrule=center,pavucontrol
windowrule=float,blueman-manager
windowrule=size 860 580,blueman-manager
windowrule=center,blueman-manager

# Mouse
bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

# Keybinds
bind=SUPER,TAB,exec,~/.config/rofi/launcher.sh &
bind=SUPERSHIFT,TAB,exec,sh ~/.config/eww/toggle.sh &
bind=SUPER,DELETE,exec,~/.config/rofi/powermenu.sh &
bind=SUPER,ESCAPE,exec,swaylock -i $(get-theme --wallpaper)
bind=SUPER,PRINT,exec,grim -g "$(slurp)"
bind=SUPER,W,exec,firefox
bind=SUPER,N,exec,thunar
bind=SUPER,RETURN,exec,kitty -o allow_remote_control=yes --single-instance zsh
bind=SUPER,F,fullscreen,0
bind=SUPERSHIFT,Q,killactive
bind=SUPERSHIFT,SPACE,togglefloating,

bind=,XF86AudioMute,exec,pactl set-sink-mute @DEFAULT_SINK@ toggle
bind=,XF86AudioLowerVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ -2%
bind=,XF86AudioRaiseVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ +2%
bind=,XF86AudioMicMute,exec,pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind=,XF86MonBrightnessUp,exec,brightnessctl -q set 5%+ # increase screen brightness
bind=,XF86MonBrightnessDown,exec,brightnessctl -q set 5%- # decrease screen brightnes

bind=SUPER,h,movefocus,l
bind=SUPER,j,movefocus,d
bind=SUPER,k,movefocus,u
bind=SUPER,l,movefocus,r

bind=SUPER,left,resizeactive,-40 0
bind=SUPER,right,resizeactive,40 0
bind=SUPER,up,resizeactive,0 40
bind=SUPER,down,resizeactive,0 -40


# Workspaces
bind=SUPERSHIFT,h,movewindow,l
bind=SUPERSHIFT,j,movewindow,d
bind=SUPERSHIFT,k,movewindow,u
bind=SUPERSHIFT,l,movewindow,r

wsbind=1,DP-2
wsbind=2,DP-2
wsbind=3,DP-2
wsbind=4,DP-2

workspace=DP-2,1
workspace=DP-2,2
workspace=DP-2,3
workspace=DP-2,4

bind=SUPER,1,focusmonitor,DP-2
bind=SUPER,1,workspace,1
bind=SUPER,2,focusmonitor,DP-2
bind=SUPER,2,workspace,2
bind=SUPER,3,focusmonitor,DP-2
bind=SUPER,3,workspace,3
bind=SUPER,4,focusmonitor,DP-2
bind=SUPER,4,workspace,4

bind=SUPERSHIFT,1,movetoworkspacesilent,1
bind=SUPERSHIFT,2,movetoworkspacesilent,2
bind=SUPERSHIFT,3,movetoworkspacesilent,3
bind=SUPERSHIFT,4,movetoworkspacesilent,4